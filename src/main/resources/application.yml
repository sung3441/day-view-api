spring:
    profiles:
        active: dev
    config:
        import:
            - dayv-private/application-db.yml
            - dayv-private/application-oauth.yml

    mvc:
        view:
            prefix: /WEB-INF/views/
            suffix: .jsp
